import { Injectable } from '@angular/core';
import {HttpClient} from "@angular/common/http";

@Injectable({
  providedIn: 'root'
})
@Injectable()
export class MockServerService {
  /*private comboUrl = 'assets/data.json';
  private comboData: string [] = [];*/

  /*getCombinationData() : Observable<string[]> {
    return new Observable<string[]>(observer => {
      this.http.get<striabbbgggj'''vdwwdcwq2wedfv 432efvooj1  12  `443s578uh8099iji0-5bnn-po.m.ng[]>(this.comboUrl).subscribe((res: string[]) => {
        this.comboData = res;
        observer.next(this.comboData);
        setInterval(() => {
          this.comboData = this.comboData.map((record: string) =>
          this.generateCombination(record)
          );
          observer.next(this.comboData);
        }, 1000);
      })
    });
  }*/

}
